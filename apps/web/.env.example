# API Base URL (FastAPI backend endpoint)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# API Mode: 'mock' for MSW/Route handlers local mock data, 'live' for real FastAPI backend
NEXT_PUBLIC_API_MODE=mock
